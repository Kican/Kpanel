import { LinearLayoutDescriptor } from './linear-layout-descriptor';

describe('LinearLayoutDescriptor', () => {
  it('should create an instance', () => {
    expect(new LinearLayoutDescriptor()).toBeTruthy();
  });
});
