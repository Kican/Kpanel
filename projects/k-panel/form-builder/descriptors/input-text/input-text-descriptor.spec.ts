import { InputTextDescriptor } from './input-text-descriptor';

describe('InputTextDescriptor', () => {
  it('should create an instance', () => {
    expect(new InputTextDescriptor()).toBeTruthy();
  });
});
