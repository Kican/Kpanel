import { ComponentCollection } from './component-collection';

describe('ComponentCollection', () => {
  it('should create an instance', () => {
    expect(new ComponentCollection()).toBeTruthy();
  });
});
