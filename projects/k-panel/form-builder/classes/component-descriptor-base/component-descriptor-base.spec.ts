import { ComponentDescriptorBase } from './component-descriptor-base';

describe('ComponentDescriptorBase', () => {
  it('should create an instance', () => {
    expect(new ComponentDescriptorBase()).toBeTruthy();
  });
});
