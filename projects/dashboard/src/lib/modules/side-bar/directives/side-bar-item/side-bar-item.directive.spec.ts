import { SideBarItemDirective } from './side-bar-item.directive';

describe('SideBarItemDirective', () => {
  it('should create an instance', () => {
    // const directive = new SideBarItemDirective();
    // expect(directive).toBeTruthy();
  });
});
