import { DtSortableDirective } from './dt-sortable.directive';

describe('DtSortableDirective', () => {
  it('should create an instance', () => {
    const directive = new DtSortableDirective();
    expect(directive).toBeTruthy();
  });
});
