import {SidebarContainerDirective} from './sidebar-container.directive';

describe('SidebarContainerDirective', () => {
	it('should create an instance', () => {
		const directive = new SidebarContainerDirective();
		expect(directive).toBeTruthy();
	});
});
